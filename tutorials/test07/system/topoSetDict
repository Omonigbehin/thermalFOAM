FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}
actions
(
    {
        name    f_empty;
        type    faceSet;
        action  new;
        source  boxToFace;
        sourceInfo
        {
            box (0 0 0) (0 0 0);   // empty box => empty set
        }
    }
);
