FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}
pointSync  false;

patches
(
    {
        name            erodedWall;
        patchInfo
        {
            type        wall;
        }
        constructFrom   set;
        set             f_empty;    // comes from topoSet
    }
);
